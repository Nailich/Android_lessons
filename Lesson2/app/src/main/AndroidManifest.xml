<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.stonefist.lesson2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="Activity_main_fio" android:name=".FioActivity"></activity>
        <activity android:label="Activity_main_contacts" android:name=".ContactsActivity"></activity>
        <activity android:label="skills" android:name=".SkillsActivity"></activity>
        <activity android:label="video" android:name=".VideoActivity"></activity>
    </application>

</manifest>